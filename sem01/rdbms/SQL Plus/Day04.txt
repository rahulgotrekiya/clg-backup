1. Create a table called “Clients” from the “Client_Master” table.

CREATE TABLE CLIENTS AS SELECT * FROM CLIENT_MASTER WHERE 1=0;

2. Insert all records of “Client_Master” into “Clients”

INSERT INTO CLIENTS SELECT * FROM CLIENT_MASTER WHERE;

3. Create table “Products” from “Product_master”

CREATE TABLE PRODUCTS AS SELECT * FROM PRODUCT_MASTER WHERE 1=0;

4. Copy only selected Product’s information into “Products” table.

INSERT INTO PRODUCTS SELECT * FROM PRODUCT_MASTER WHERE QTY_HAND = 100;

5. Create table “Salesman” from “Salesman_Master” where “Salesman” table contains Fields: Salesman number, Name, Address1, City, Target_Get, Achieved_Target

CREATE TABLE SALESMAN AS SELECT S_NO, NAME, ADD1, CITY, TARGET, SALES FROM Salesman_Master WHERE 1=0;

6. Insert all records for the fields in new table from “Salesman_Master” table

INSERT INTO SALESMAN SELECT S_NO, NAME, ADD1, CITY, TARGET, SALES FROM SALESMAN_MASTER;

7. Create an “Order_Master” table form “Sales_Order” Table

CREATE TABLE ORDER_MASTER AS SELECT * FROM SALES_ORDER WHERE 1=0;

8. Insert appropriate records in “Order_Master” from Sales_Order” table

INSERT INTO ORDER_MASTER SELECT * FROM SALES_ORDER WHERE ORDER_STATUS <> 'Fulfilled';

9. Create “Order_Detail” table from “Sales_Order_Detail” table.

CREATE TABLE ORDER_DETAILS AS SELECT * FROM Sales_Order_Details WHERE 1=0;

10. Insert few selected records in “Order_Detail” from Sales_Order_ Detail” table.

INSERT INTO ORDER_DETAILS SELECT * FROM Sales_Order_Details WHERE PRODUCT_RATE <= 1500;

11. Destroy tables: Client_Master, Product_Master and Salesman_Master table using Truncate

TRUNCATE TABLE  Salesman_Master;
TRUNCATE TABLE Product_Master;
TRUNCATE TABLE Client_Master;

12. Destroy tables: “Sales_Order” and Sales_Order_Details using drop operation.

DROP TABLE Sales_Order_Details;
DROP TABLE Sales_Order;

14. Copy only the structure of Applicant Table into Applicant_Master

CREATE TABLE Applicant AS SELECT * FROM Applicant_Master WHERE 1=0;

15. Create a new table called Course_Master having same fields as Enterance_Test table but change the column name as Course_Code, Course_Name and Course_Credit

CREATE TABLE Course_Master(Course_Code, Course_Name, Course_Credit) AS SELECT * FROM ENTRANCE_TEST WHERE 1=0;

16. Insert new data in Course_Master table.

INSERT INTO COURSE_MASTER SELECT ET_ID, ET_NAME, MAX_SCORE FROM ENTRANCE_TEST;

17. Create a view of Entrance_Test_Details table having only 2 columns

CREATE VIEW ETEST_DETAILS_VIEW AS SELECT AID, ETID FROM ETEST_DETAILS;

18. Create a view of Applicant Table

CREATE VIEW Applicant_view as SELECT * FROM Applicant_Master;

19. Change the record (Column values) in the view table.

UPDATE APPLICANT_VIEW SET A_NAME = 'RAHUL GOTREKIYA' WHERE AID = 'A001';

20 Remove the view.

