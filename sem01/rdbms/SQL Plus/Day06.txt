1. Use following tables and solve given queries below it. 
 
APPLICANT (AID, A_Name, City, B_Date) ENTRANCE_TEST (ET_ID, ET_Name, Max_Score) ETEST_DETAILS (AID, ETID, ETest_Date, Score) 

1. How many applicants have appeared for each test.

INSERT INTO ETEST_DETAILS VALUES ('A010', 'T004', '15-MAY-24', 100, 'FSD');

SELECT ETID, COUNT(*) FROM ETEST_DETAILS GROUP BY ETID;

2. Display highest score for each test 

SELECT ETID, MAX(SCORE) FROM ETEST_DETAILS GROUP BY ETID;

3. Display applicant’s ID who appeared for more than 3 tests 

INSERT INTO ETEST_DETAILS VALUES ('A009', 'T001', '15-MAY-24', 89, 'FSHHD');
INSERT INTO ETEST_DETAILS VALUES ('A009', 'T002', '15-MAY-24', 10, 'FSHD');
INSERT INTO ETEST_DETAILS VALUES ('A009', 'T003', '15-MAY-24', 80, 'FSGD');

SELECT AID, COUNT(*) AS TOTAL_TEST FROM ETEST_DETAILS GROUP BY AID HAVING COUNT(*) > 3;

4. Calculate applicant’s average score across all test they have appeared in 

SELECT AID, AVG(SCORE) AS average_score FROM ETEST_DETAILS GROUP BY AID;

5. Display number of applicants by city 

INSERT INTO APPLICANT VALUES('A011', 'TEST', '123 MG Road, Delhi', '10-NOV-01');
SELECT CITY, COUNT(*) FROM APPLICANT GROUP BY CITY;

6. Display ETID and Average score where average score is more than 50.

SELECT ETID, AVG(SCORE) AS AVG_SCORE FROM ETEST_DETAILS GROUP BY ETID HAVING AVG(SCORE) > 50;

7. Count date wise total entrance test to be held. 

SELECT ETEST_DATE, COUNT(ETID) AS TESTS FROM ETEST_DETAILS GROUP BY ETEST_DATE;
 
2. Use the following tables and solve below given queries. 
 
Distributor (Dno, DName, City, Phone) Item (Item_No, Item_Name, Price, Weight) Dist_Item (Dno, Item_No, Qty, Date) 
 
1. Display city wise total number of distributors 

INSERT INTO Distributor VALUES ('D006', 'ABC Distributors', 'Kolkata', 9876543212);

SELECT CITY, COUNT(*) AS Distributors FROM Distributor GROUP BY CITY;

2. List distributors’ no by who distributed more than 50 items in month of July 

SELECT DNO, SUM(QTY) FROM DIST_ITEM WHERE TO_CHAR(DATE_SUPPLIED, 'MON') = 'JUL' GROUP BY DNO HAVING SUM(QTY) > 50;

3. List Itemwise  total quantity delivered and display items having more than 5000 Qty delivered

SELECT ITEM_NO, SUM(QTY) FROM DIST_ITEM GROUP BY ITEM_NO HAVING SUM(QTY) > 5000 ; 

4. List Dno who delivered more than 50 items for each month 

SELECT DNO,TO_CHAR(DATE_SUPPLIED, 'MON'), SUM(QTY)  FROM DIST_ITEM GROUP BY DNO, TO_CHAR(DATE_SUPPLIED, 'MON')

5. Display item details in descending order of price and ascending order of weight 

SELECT * FROM ITEM ORDER BY (WEIGHT ascc);

6. Show all distributors in alphabetical order of City and DName 

7. Calculate average quantity of items distributed on each day. 
8. Find the weight wise average price of items

3. Use the following tables and solve below given queries. 
 Worker (Id, Name, Wages_Per_Hr) Job (Job_Id, Job_Type) Assigned (W_Id, J_Id, Start_Date, Status, Total_Hrs) 
 
1. Display all workers in descending order of their Wages_Per_Hr 
2. Count workers in each J_Id 
3. List all the W_Id who has worked for more than 100 hrs 
4. Count total jobs which are pending till date 5. Display all Job_Id according to alphabetical order of their type 
 

